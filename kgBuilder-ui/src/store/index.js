import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

/**
 * 应用状态管理（Vuex）
 *
 * - 预留 `state`/`mutations`/`actions`/`modules` 扩展位
 */
export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {}
});
